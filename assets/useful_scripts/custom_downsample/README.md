# Custom downsampling of sequencing depths

In some circumstances it is useful to downsample samples or genes to ensure that the resolution at which we are comparing the different samples is the same.

With this aim, we provide a couple of notebooks to do this for the depths.

## Mutations

Use `downsample.ipynb`

## Depths

Use `downsample_depths.ipynb`
